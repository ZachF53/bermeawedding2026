# Django
SECRET_KEY=replace-with-a-long-random-string
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1,brianandaisha.com,www.brianandaisha.com

# Email — SendGrid (preferred)
SENDGRID_API_KEY=your_sendgrid_api_key_here
DEFAULT_FROM_EMAIL=zacherylong@aspiredwebsites.com

# Email (SMTP fallback) — only used if EMAIL_BACKEND is switched to SMTP
EMAIL_HOST=smtp.sendgrid.net
EMAIL_PORT=587
EMAIL_HOST_USER=apikey
EMAIL_HOST_PASSWORD=your-app-password
EMAIL_USE_TLS=True

# Where RSVP notifications are sent
NOTIFICATION_EMAIL=bermeawedding@outlook.com

# Hand-shake secret echoed by the RSVP form to filter scrapers
RSVP_SECRET=4uSTHu0SsCu5olLeBEPI19n0W7TQhWtj
